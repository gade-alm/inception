server {
		listen 443 ssl;
		server_name	www.gade-alm.42.fr gade-alm.42.fr;

		ssl_protocols TLSv1.3;
		ssl_certificate /etc/nginx/ssl/ssl.crt;
		ssl_certificate_key /etc/nginx/ssl/ssl.key;

		root /var/www/html;
		index index.php index.html;
		location / {
			try_files $uri =404;
		}
	}
